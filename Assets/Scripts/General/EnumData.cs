using UnityEngine;

public static class EnumData 
{
    public enum GameMode
    {
        outdors,
        trade,
        inventory,
        dialog,
        craft,
        storage,
        menu
    }
    public enum ToolsType
    {
        bag,
        wrench,
        hacksaw,
        mask,
        crowbar
    }
}
